// Intentionally minimal placeholder to keep the agreed structure.
// Not imported or used (we rely on src/config/app.config.ts as the single source of truth).

export const __appInfoDefaults__ = Object.freeze({});
